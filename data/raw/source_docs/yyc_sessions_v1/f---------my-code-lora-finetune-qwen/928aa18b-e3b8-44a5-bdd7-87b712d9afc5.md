<!-- session=928aa18b-e3b project=f---------my-code-lora-finetune-qwen ts=2026-05-31 18:10 -->

<!-- role=assistant ts=2026-05-31 18:17 -->

已经读完官方 `install.md`。这是一个面向 Nature 期刊学术写作 / 科学绘图的 skill 仓库（10 个 skill：nature-figure / polishing / writing / reviewer / citation / data / response / paper2ppt / reader / academic-search），原始设计目标是 Codex。

对 Claude Code，官方推荐的安装方式是：
1. 把仓库 clone 到一个稳定的本地路径（不要拆散）
2. 在 `~/.claude/agents/` 或 `~/.claude/commands/` 里放**很薄的 wrapper**，wrapper 里 `Read` 对应 skill 的 `SKILL.md`
3. 这样可以保留 `references/ / static/ / manifest.yaml / _shared/` 的相对引用，`git pull` 就能升级

在动手前需要先和你确认几个点：

<!-- role=assistant ts=2026-05-31 18:31 -->

安装完成。汇总如下。

## 已安装

**仓库克隆**：[C:\Users\[USER]\ai-skills\nature-skills\](C:/Users/[USER]/ai-skills/nature-skills/)

**10 个 wrapper 已写入 `C:\Users\[USER]\.claude\agents\`**（全局 subagent，对所有项目可见）：

10 个 wrapper 指向的 SKILL.md 全部存在；frontmatter 含 `name` + `description`，body 按 install.md 4.3 节模板写：先 `Read` 主 SKILL.md，按需读 `references/ / static/ / manifest.yaml / _shared/`。

## 如何使用

新开一个 Claude Code 会话后（当前会话**不会**热加载新 subagent，必须重启），可以这样触发：

或者直接用自然语言描述任务，Claude Code 会按 wrapper 里的 description 自动挑选。

## 后续维护

升级：`cd C:\Users\[USER]\ai-skills\nature-skills; git pull`。wrapper 指向稳定路径，无需重装。

注意点：
- 路径用了 Windows 绝对路径而非 `~/ai-skills/...`，避开 `~` 在 Windows 上的展开问题。
- 这些 skill 主要面向 Nature 期刊英文论文写作 / 绘图 / 审稿；当前 LoRA 课设主线是中文数学题，**短期内大概率用不上**——除非后续要写英文论文或做高质量结题图表，那时 `nature-figure` 和 `nature-paper2ppt` 会比较有用。
