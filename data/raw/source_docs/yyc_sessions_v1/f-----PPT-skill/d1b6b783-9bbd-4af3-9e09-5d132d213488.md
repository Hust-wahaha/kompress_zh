<!-- session=d1b6b783-9bb project=f-----PPT-skill ts=2026-06-10 21:24 -->

<!-- role=user ts=2026-06-10 21:24 -->

你是这个 skill 的资深审查者。它即将开源，面向零代码基础的大学生。请在开源前做一次彻底、对抗性的代码与设计审查，把所有问题找出来。

【第一步：先理解再批判】
先通读整个代码库（SKILL.md、全部脚本、agent 定义、Typst 模板、README），建立完整心智模型。先用几句话复述"这个 skill 实际是怎么工作的"，确认你读懂了全局，再开始挑问题。把 TODO.md（设计规格）当作"应有行为"的标尺，逐条核对代码是否真的兑现了规格里的不变量。

【审查维度，不限于此，鼓励发现清单外的隐患】
- 正确性：逻辑错误、边界条件、异常处理、静默失败。
- 设计不变量是否被代码真正兑现：
  · 抽取与生成严格分离；所有 AI 生成处是否都打了对应的 AI 标。
  · 溯源：知识点是否都挂在 (源文件名,源内页序号) 原始锚点上；指针是否按"参考基准"正确换算；L0 来源戳是否落实。
  · 构建顺序：L0/页码映射是否确实先建、其余产物依赖它。
  · 文科默认保守压缩；图是否原样抠取而非重绘。
  · 主控是否真的不加载原文（token 有界）——有没有把整份文件塞进上下文的地方？
  · 开工前 Phase 0–2 的用户确认是否真会阻塞，不会被绕过直接开工。
- 鲁棒性（真实脏输入）：无章节命名/命名混杂、.ppt vs .pptx、无文字层 PDF、损坏文件、LibreOffice 未装、中文字体缺失、空文件夹、超大课件——是否优雅降级而非崩溃/乱码。
- 安全：输入文件夹是否严格只读（绝不改用户原件）；Typst 二进制下载是否校验来源/完整性；shell 调用与路径处理是否稳妥；无破坏性操作。
- doctor/bootstrap：跨平台(Win/mac/Linux)、免 root、幂等可重入、字体检测、失败时给人话指引。
- 可恢复性与确定性：中途崩溃能否断点续跑？输出是否可复现？
- 复用性/DRY：重复代码、可抽公共模块、职责边界是否清晰。
- 工作流/架构：流程能否更简、更稳、更省 token；agent 划分是否合理。
- 用户视角（零代码学生）："一键"承诺是否真成立；交互话术与默认值是否友好；报错是否人话且可操作。
- 信赖/透明：是否让用户看到 L0 删了哪些页并可复核；AI 生成内容是否显眼标注；溯源对用户是否可见可用。
- 产物质量：6 个 PDF 实际渲染是否过关（Typst 中文、多栏溢出、脑图过宽、索引可读性）。
- 文档：README 能否让零基础用户从零跑起来（前置/快速开始/常见报错排查）；SKILL.md 触发描述是否准确（不漏触发、不误触发）；是否有示例。

【交付方式】
产出一份分级审查报告，按严重度排序：① 开源前必修 ② 建议修 ③ 锦上添花。每条给出：问题描述、所在 文件:行、为什么是问题、具体修复建议（能给 diff 更好）。先只出报告，等我确认后再批量改；高风险项不要擅自改动。

【态度】
做对抗性 red-team，主动找茬，宁可多报。所有判断都基于真实读到的代码，不要靠假设。规格没覆盖到的隐患也要提出来。

<!-- role=assistant ts=2026-06-10 21:37 -->

现在我有了完整的代码视图。以下是系统性审查报告。

---

## 开卷考课件整理 Skill — 对抗性审查报告

**心智模型复述（确认已读懂）**：Skill 是一个编排器，分 11 个 Phase 自动把课件文件夹转成 6 种 PDF。核心架构：Python 脚本负责确定性工作（抽取、合并、渲染），主控 Claude 不加载原文，只读脚本打印的简报；AI 内容判断通过"切包→子 agent 只读一章→merge 合并"完成。所有知识点内部挂 `(源文件名, 源内页序号)` 锚点，显示指针在渲染时按参考基准换算。锚点在 `distill_l1.py` 写入 pointer 字符串后即被 `del sec["anchors"]` 删除。

---

## ① 开源前必修

---

### 【M1】`distillation.md` agent 指令与实际流水线完全矛盾 — 会导致数据竞争/覆盖

**文件**：[agents/distillation.md](agents/distillation.md):11–12

**原文**：

**问题**：实际流水线（`SKILL.md` Phase 6b 步骤三、`merge_ai_l1.py` 全文）要求子 agent 写 `work/distilled/ai/out_<k>.json`，由 `merge_ai_l1.py` 做确定性合并。但 agent 卡告诉 agent 直接写回 `l1.json`。后果：

- 若 agent 照做，会用一章的内容覆盖整个 `l1.json`，其他章全部丢失。
- 若多章并行，多个 agent 同时写同一文件，产生竞争。
- 连 `merge_ai_l1.py` 的存在理由都被架空。

**修复**：将 `distillation.md` 的"产出"部分改为：

---

### 【M2】`distillation.md` 写的输入路径错误 — agent 会读错文件

**文件**：[agents/distillation.md](agents/distillation.md):7–9

**原文**：

**问题**：`prep_ai_distill.py` 已经把每章内容过滤（跳过废页、只保留必要字段、去掉图片二进制）后切成 `work/distilled/ai/in_<k>.json`。如果 agent 直接读 `content.json`，它会：

1. 不知道该读哪个 stem（一章可能有多个源文件）；
2. 包含废页（`decorative=true` 的幻灯片），违背"跳过废页"原则；
3. 包含备注字段（`notes`），L1 输入包本来不应含备注。

**修复**：将输入改为：

删除 `content.json` 的引用。

---

### 【M3】`verify_l1.py` 锚点检查是死代码——永远报"全绿"

**文件**：[scripts/verify_l1.py](scripts/verify_l1.py):28–39、[scripts/distill_l1.py](scripts/distill_l1.py):161

**代码**（distill_l1.py 第 161 行）：

**代码**（verify_l1.py 第 30–39 行）：

`sec["anchors"]` 已被删除，`sec.get("anchors")` 始终返回 `None`，循环从不执行，`bad_anchor` 始终为空，最后输出"所有锚点可解析、content.json 存在"——这是永久假阳性。AI 写错锚点（如 `chapter1.ppt#s999`）不会被发现。

**修复选项 A**：在 `distill_l1.py` 中保留 `anchors` 字段（改名 `source_anchors` 避免与 Typst 模板冲突）。  
**修复选项 B**（最小改动）：`verify_l1.py` 改为从 `pointer` 字段反向解析幻灯片编号并验证。

---

### 【M4】`doctor.py` macOS/Linux 安装失败无回退，README 声称三平台均可

**文件**：[scripts/doctor.py](scripts/doctor.py):91–100、[README.md](README.md):126

**代码**：

`winget` 不存在于 macOS/Linux。`_run` 捕获 `FileNotFoundError` 后打印错误并返回 127，不中止；二次 `diagnose()` 发现仍然 ❌ 但没有给出任何可操作提示（没有 `brew install libreoffice`、`apt install libreoffice` 等）。README 第 126 行写"Windows / macOS / Linux 均可"——这是误导。

**修复**（最小改动）：

---

### 【M5】`render_m.py` 的 `matplotlib` 未在 `doctor.py` 检测，缺失时渲染 M 时崩溃

**文件**：[scripts/doctor.py](scripts/doctor.py):32–68、[scripts/render_m.py](scripts/render_m.py):20–23

`render_m.py` 第 20 行无保护地 `import matplotlib`，但 `doctor.py` 的 `diagnose()` 完全不检测 `matplotlib`；`all_ok` 计算也不含它。用户首次运行到 Phase 10（渲染 M）时会收到裸 `ImportError`，不知道原因。

**修复**：在 `doctor.py diagnose()` 中添加：

并在 `install_missing` 中添加 `"matplotlib"` 到 pip 安装列表。

---

### 【M6】`render.py --open` 在 macOS/Linux 崩溃

**文件**：[scripts/render.py](scripts/render.py):84–85

`os.startfile` 是 Windows 专有 API，在 macOS/Linux 不存在（`AttributeError`）。

**修复**：

---

### 【M7】`render.py` Typst 子进程在 Windows 可能 UnicodeDecodeError

**文件**：[scripts/render.py](scripts/render.py):50–52

`text=True` 不指定 `encoding`，在 Windows 上默认 GBK。Typst 若输出含中文路径的错误信息（UTF-8 编码），会导致 `UnicodeDecodeError`，渲染错误信息无法显示。对比 `extract.py` 第 76–77 行已正确处理同样问题（`encoding="utf-8", errors="replace"`）。

**修复**：

---

### 【M8】`render_m.py` CJK 字体硬编码，不读 `config.json`——Linux/国产OS 中文乱码

**文件**：[scripts/render_m.py](scripts/render_m.py):30–31

这与所有其他产物（L1/QA/L2/C）通过 `meta.cjk_font` 从 `config.json` 读取字体的方式不一致。在 Linux 上，只有 "Noto Sans CJK SC" 或 "Source Han Sans"，上述三个字体均不存在，matplotlib 回退到 DejaVu Sans，思维导图中所有中文显示为方块。

**修复**：读 `m.json` 中的 `meta.cjk_font`，插入到 `font.sans-serif` 首位：

---

### 【M9】`build_l0.py` 书签指针偏移：全废页的源文件书签指向上一章末页

**文件**：[scripts/build_l0.py](scripts/build_l0.py):137–143

若某源的所有幻灯片都被判定为废页（`decorative=True`），`final_page` 在处理该源时不增加。书签被记录指向 `final_page`（上一源的最后一页）。当用户在 L0.pdf 中点击该章书签时，跳到的是上一章的末页，而非本章开头。

**修复**：改为后置记录——处理完所有页后，若有新页被写入则记录书签，否则标记书签为 "无可用页"：

---

### 【M10】同名不同目录的源文件（递归扫描时）静默覆盖 `work/extracted/`

**文件**：[scripts/extract.py](scripts/extract.py):222–223

若递归扫描发现 `chap1/第一章.pptx` 和 `chap2/第一章.pdf`，两者 stem 均为 `第一章`，第二个处理会覆盖第一个的 `content.json` 和 `images/`，且无任何警告。这是静默的数据损坏。

**修复**：检测冲突并报错或自动加后缀：

---

## ② 建议修

---

### 【S1】`extract.py` docstring 标注 "Phase 3"，SKILL.md 叫它 "Phase 4"

**文件**：[scripts/extract.py](scripts/extract.py):1（`Phase 3 抽取`）  
SKILL.md 第 83 行对应位置是 Phase 4（Phase 3 = bootstrap）。文档不一致，会让接手维护的人困惑。  
**修复**：统一为 Phase 4。

---

### 【S2】`_decorative_hint` 在同一张幻灯片中被调用两次

**文件**：[scripts/extract.py](scripts/extract.py):268–271

每张幻灯片调用两次，浪费。应缓存结果：

---

### 【S3】`distill_l1.py` reference_base=l0 无前置检查

**文件**：[scripts/distill_l1.py](scripts/distill_l1.py):100–101

若用户选了 `reference_base=l0` 但尚未运行 `build_l0.py`（L0 还没生成），`l0map` 为 `{}`，所有指针都显示为 `"L0 p.None"` 或只有名字没有页码。  
**修复**：当 `reference_base=l0` 且 `L0_PAGEMAP_PATH` 不存在时，打印清晰警告：

---

### 【S4】`has_cjk_font()` 用子串匹配，可能误报

**文件**：[scripts/lib/common.py](scripts/lib/common.py):219–221

Windows 注册表值形如 `"Microsoft YaHei & Microsoft YaHei UI (TrueType)"`，`"Microsoft YaHei"` 会正确匹配。但 `"SimHei"` 也会匹配 `"SimHeiBold (TrueType)"`（若存在）等衍生条目，不影响功能但语义不精确。  
更重要的缺陷：注册表值名是 `"字体名 (TrueType)"` 形式，而 Typst 需要的是字体族名。doctor 通过不代表 Typst 一定找得到同名字体族。  
**修复**：低优先级，可在报告中注明"字体检测为尽力而为"。

---

### 【S5】`README.md` 的 `/skills add` 命令不存在

**文件**：[README.md](README.md):139–140

Claude Code 没有 `/skills add` 命令。SKILL.md 本身（第 397–399 行）说要手动复制文件到 `~/.claude/skills/`。这会让零代码用户卡在第一步。  
**修复**：给出明确的复制命令：

---

### 【S6】`merge_ai_l1.py` 的 `chapters[k - 1]` 假设章序连续

**文件**：[scripts/merge_ai_l1.py](scripts/merge_ai_l1.py):123

`k` 从 1 开始，假设 `chapters[0]` 是第一章，`chapters[1]` 是第二章……这依赖 `prep_ai_distill.py` 和 `distill_l1.py` 的 `chapter_blocks()` 顺序完全一致。目前两者共用相同逻辑，是安全的。但若未来修改了其中一个的枚举顺序，此处会静默错乱而不报错。  
**建议**：改为按 `chapter_title` 匹配而非位置匹配。

---

### 【S7】扫描目录无规模保护，误指 C:\\ 会卡住数分钟

**文件**：[scripts/inventory.py](scripts/inventory.py):84–88

递归扫描无文件数量上限和时间限制。用户若不小心给了 `C:\` 或 `/home`，会触发遍历成千上万个目录，且过滤后才统计文件数，中途没有进度提示。  
**修复**：设硬上限并提前退出：

---

### 【S8】`l1.typ` `first` 变量在 Typst 中的作用域

**文件**：[templates/l1.typ](templates/l1.typ):124–132

Typst 的 `let` 创建不可变绑定，`first = false` 在 Typst 中实际上是在块作用域内创建新绑定而不会修改外层变量。在 Typst 0.11+ 中这可能触发警告，或者 `chapter-head` 永远收到 `first=true`，导致每章前都 `pagebreak(weak: true)`（不过这里 `first=true` 时不 pagebreak，`first=false` 时才 pagebreak，实际效果可能是第一章也被 pagebreak——需要实测）。

同样的问题存在于 `l2.typ:111`、`qa.typ:118`、`c.typ:147`。

**修复**：使用计数器替代布尔标志：

---

### 【S9】`coverage.md` 对 AI 生成标记的检查过于宽泛

**文件**：[agents/coverage.md](agents/coverage.md):8

coverage.md 只做"统计 + 提示"，没有规定检查深度。实际上 `l2.json`、`c.json` 中也存在来自 AI 的内容（虽然这些 agent 被要求不补充），且若 agent 没遵守 `ai_answer: false` 规则，这里也不会发现。  
**修复**：明确 coverage 必须检查 `l2.json` 和 `c.json` 是否有 `definition` 不为空但实为 AI 补充的条目（目前没有字段标记这一点，这是设计盲区）。

---

## ③ 锦上添花

---

### 【L1】断点续跑机制未对用户透明

`extract.py` 有单文件缓存（`out.exists() and out.stat().st_mtime >= src.stat().st_mtime`），`distill_l1.py` 覆盖写 `l1.json`，各 merge 脚本覆盖写对应 JSON——中途崩溃后重跑会重新处理。这是有限的幂等性，但 README 和 SKILL.md 都没有说明"某一步失败后应该从哪里重跑"，用户可能从头重来浪费时间。  
**建议**：在 SKILL.md 每个 Phase 后加一句"若本步失败，只需重跑此命令即可"。

---

### 【L2】`render_m.py` 降级截断的 "+N" 提示用户不可读

**文件**：[scripts/render_m.py](scripts/render_m.py):349–352

当节点太多时，末节点后追加 ` +3` 等后缀，表示"还有 3 个节点未显示"。这对零代码用户不直观。  
**建议**：改为在该分支末尾加一行浅灰文字"…另有 3 条（见 L1 手册）"。

---

### 【L3】`mindmap_global.md` 缺少最小化文档

**文件**：[agents/mindmap_global.md](agents/mindmap_global.md)

探索时 Explore agent 读到了这个文件，但其内容没有被单独读取进入审查（文件确实存在）。从目录树知道有该文件，但未确认其 schema 是否与 `merge_m.py` 消费的 `out_global.json` 格式一致。建议在开源前单独验证 `out_global.json` 的 `course_title` 字段对应正确。

---

### 【L4】`README.md` 缺少常见错误排查章节

面向零代码学生，以下场景有必要在 README 中给出说明：
- `LibreOffice 转换失败` → 确认 LibreOffice 已安装，路径无中文空格
- `Typst 渲染失败：font not found` → 运行 `doctor.py` 确认字体
- `PPT 文字抽取近零（疑似扫描件）` → 说明本 skill 不支持 OCR
- `work/ 下没有 distilled/ai/out_<k>.json` → 说明子 agent 需要明确被派发

---

**严重度总结**

最高优先级三条：**M1**（agent 卡会写坏数据）、**M3**（verify 沉默失效）、**M5**（M 产物首次使用必崩）。这三条在"一键"承诺的实际链路上，会让零代码用户遭遇无法自行诊断的故障。
