<!-- session=5cc97455-fc0 project=-home-zbkuang-ppt-parsing-hecheng-pipline-llm ts=2026-06-11 19:12 -->

<!-- role=assistant ts=2026-06-11 19:12 -->

现在我已经有了完整的图景。以下是与 [llm_rewrite_deepseek.py](llm_rewrite_deepseek.py) 直接相关的文件，以及整个 pipeline 中各文件的作用：

---

## 直接依赖（import 关系）

---

## Pipeline 上下游（数据流关系）

整个流程按 [SYNTHESIS_PIPELINE.md](SYNTHESIS_PIPELINE.md) 描述的顺序运行：

---

## 其余文件简述

<!-- role=assistant ts=2026-06-11 19:35 -->

文件已写到 `/tmp/llm_rewrite_local.py`，用以下命令复制到项目目录：

---

## 主要改动说明

相比 [llm_rewrite_deepseek.py](llm_rewrite_deepseek.py)，只改了以下几处：

**新增 `AVAILABLE_MODELS` 字典**，直接映射模型名到本地路径：

**`call_llm` 改用 vLLM 推理**（替换了 `openai.OpenAI` 调用）：
- 用 `AutoTokenizer.apply_chat_template` 拼 prompt，支持 Qwen3 的 `enable_thinking` 参数
- 用 `vllm.LLM.generate()` 做离线推理
- 自动剥离 `<think>...</think>` 思维链后再解析 JSON

**新增 CLI 参数**：
- `--model`：选 `qwen3.5-27b` 或 `qwen3.5-35b`（默认 27B）
- `--model-path`：直接指定路径，覆盖 `--model`
- `--max-new-tokens`：控制生成长度（默认 4096）
- `--tensor-parallel-size`：多卡并行（默认 1）
- `--enable-thinking`：开启 Qwen3 思维链（默认关闭，保持 JSON 输出干净）

Prompt、校验逻辑、断点续跑逻辑与原版完全一致。

<!-- role=assistant ts=2026-06-11 19:46 -->

结论如下，可以删掉 **6 个**：

**明确的旧版本（被更新版直接取代）：**

**不属于重写 pipeline 的独立工具：**

`ppocr.py` 和 `split_markdown_by_toc.py` 虽然是上游步骤，但属于 pipeline 的 step 0，建议保留。

执行删除：

要我直接执行吗？

<!-- role=assistant ts=2026-06-11 21:48 -->

需要替换的路径：
- 所有 `/home/[USER]/ppt_parsing/hecheng_pipline_llm/` 前缀的脚本路径 → 直接用脚本名（从工作目录运行）
- `/home/[USER]/ppt_parsing/hecheng_pipline_llm/synthesis_output/` / `toc_split_output` 等数据路径 → 相对路径
- step 0 中引用的旧目录 `hecheng_pipline/split_markdown_by_toc.py` → 用本地副本
- 顺带修正 line 132 中提到已删除的 `render_docx_with_reference.py` → `render_docx_with_reference_table_styles.py`
- 填写默认工作目录
